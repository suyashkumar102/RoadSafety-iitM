# Frontend

React/Vite PWA scaffold for RoadSoS.

## Planned commands

```powershell
cd frontend
npm install
npm run dev
```

## Ownership

Sidhesh owns the mobile-first emergency flow, offline cache, trust UI, and assistant experience. Contract changes must be coordinated through `contracts/`.

